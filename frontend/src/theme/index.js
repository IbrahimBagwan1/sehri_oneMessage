/**
 * theme/ — one-line import surface for the design system.
 *
 * Prefer:
 *   import { colors, type, space, radius } from '../../theme';
 *
 * over deep imports into ./tokens directly.
 */
export * from './tokens';
